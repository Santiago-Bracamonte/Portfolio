// Set this to your deployed backend endpoint (Cloudflare Worker, Vercel Function, etc.)
// GitHub Pages is static, so /api/contact won't work there.
window.CONTACT_API_URL = "https://portfolio-contact.portfoliosantiago.workers.dev/api/contact";
