(function(window){
  var lightbox = window.lightbox || {};

  if (typeof lightbox.option !== 'function') {
    lightbox.option = function() {};
  }

  if (typeof lightbox.init !== 'function') {
    lightbox.init = function() {};
  }

  window.lightbox = lightbox;
})(window);
