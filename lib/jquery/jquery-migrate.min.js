(function(factory){
  if (typeof define === 'function' && define.amd) {
    define(['jquery'], factory);
  } else {
    factory(window.jQuery);
  }
})(function($){
  if (!$) return;

  if (!$.migrateVersion) {
    $.migrateVersion = '3.4.1';
  }

  if (!$.fn.andSelf) {
    $.fn.andSelf = $.fn.addBack;
  }
});
